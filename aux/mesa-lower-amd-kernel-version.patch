diff --git a/src/amd/common/ac_gpu_info.c b/src/amd/common/ac_gpu_info.c
index d9d3fd90157..e7f7306b934 100644
--- a/src/amd/common/ac_gpu_info.c
+++ b/src/amd/common/ac_gpu_info.c
@@ -277,7 +277,8 @@ ac_fill_compiler_info(struct radeon_info *info, const struct drm_amdgpu_info_dev
       out->sgpr_alloc_granularity = 8;
    }
 
-   if (device_info && device_info->num_shader_visible_vgprs) {
+   /* Some GPU info was broken before DRM 3.45.0. */
+   if (info->drm_minor >= 45 && device_info && device_info->num_shader_visible_vgprs) {
       /* The Gfx10 VGPR count is in Wave32, so divide it by 2 for Wave64.
        * Gfx6-9 numbers are in Wave64. CDNA also includes Accumulation VGPRs.
        */
@@ -487,7 +488,7 @@ ac_fill_hw_ip_info(struct radeon_info *info, const struct drm_amdgpu_info_device
    }
 
    /* Gfx6-8 don't set ip_discovery_version. */
-   if (ip_info->ip_discovery_version) {
+   if (info->drm_minor >= 48 && ip_info->ip_discovery_version) {
       info->ip[ip_type].ver_major = (ip_info->ip_discovery_version >> 16) & 0xff;
       info->ip[ip_type].ver_minor = (ip_info->ip_discovery_version >> 8) & 0xff;
       info->ip[ip_type].ver_rev = ip_info->ip_discovery_version & 0xff;
@@ -1057,6 +1058,7 @@ void ac_fill_feature_info(struct radeon_info *info, const struct drm_amdgpu_info
    info->has_sparse_image_standard_3d = info->gfx_level >= GFX9;
    info->has_sparse_unaligned_mip_size = info->gfx_level >= GFX7;
 
+   info->has_gang_submit = info->drm_minor >= 49;
    info->has_gpuvm_fault_query = info->drm_minor >= 55;
    info->has_tmz_support = device_info->ids_flags & AMDGPU_IDS_FLAGS_TMZ;
    info->uses_kernel_cu_mask = false; /* Not implemented in the kernel. */
@@ -1168,7 +1170,8 @@ void ac_fill_hw_info(struct radeon_info *info, const struct drm_amdgpu_info_devi
    info->max_sa_per_se = device_info->num_shader_arrays_per_engine;
    info->num_cu_per_sh = device_info->num_cu_per_sh;
    info->enabled_rb_mask = device_info->enabled_rb_pipes_mask;
-   info->enabled_rb_mask |= (uint64_t)device_info->enabled_rb_pipes_mask_hi << 32;
+   if (info->drm_minor >= 52)
+      info->enabled_rb_mask |= (uint64_t)device_info->enabled_rb_pipes_mask_hi << 32;
 
    info->memory_freq_mhz_effective *= ac_memory_ops_per_clock(info->vram_type);
 
@@ -1444,9 +1447,9 @@ ac_query_gpu_info(int fd, void *dev_p, struct radeon_info *info,
    assert(info->drm_major == 3);
    info->is_amdgpu = true;
 
-   if (info->drm_minor < 54) {
+   if (info->drm_minor < 35) {
       fprintf(stderr, "amdgpu: DRM version is %u.%u.%u, but this driver is "
-                      "only compatible with 3.54.0 (kernel 6.6+) or later.\n",
+                      "only compatible with 3.35.0 (kernel 4.20+) or later.\n",
               info->drm_major, info->drm_minor, info->drm_patchlevel);
       return AC_QUERY_GPU_INFO_FAIL;
    }
@@ -1588,7 +1591,8 @@ ac_query_gpu_info(int fd, void *dev_p, struct radeon_info *info,
     * with AMDGPU_GEM_CREATE_DISCARDABLE.
     */
    info->discardable_allows_big_page = info->gfx_level >= GFX10_3 && info->gfx_level < GFX12 &&
-                                       info->has_dedicated_vram;
+                                       info->has_dedicated_vram &&
+                                       info->drm_minor >= 47;
 
    /* Compute the scratch WAVESIZE granularity in bytes. */
    info->scratch_wavesize_granularity_shift = info->gfx_level >= GFX11 ? 8 : 10;
@@ -1606,6 +1610,7 @@ ac_query_gpu_info(int fd, void *dev_p, struct radeon_info *info,
                                       (!info->has_graphics && info->family >= CHIP_GFX940);
    info->max_gflops = (info->gfx_level >= GFX11 ? 256 : 128) * info->num_cu * info->max_gpu_freq_mhz / 1000;
    info->memory_bandwidth_gbps = DIV_ROUND_UP(info->memory_freq_mhz_effective * info->memory_bus_width / 8, 1000);
+   info->has_pcie_bandwidth_info = info->drm_minor >= 51;
 
    info->pcie_gen = device_info.pcie_gen;
    info->pcie_num_lanes = device_info.pcie_num_lanes;
diff --git a/src/amd/common/ac_gpu_info.h b/src/amd/common/ac_gpu_info.h
index 1ba565b5a70..af58ed2b461 100644
--- a/src/amd/common/ac_gpu_info.h
+++ b/src/amd/common/ac_gpu_info.h
@@ -380,7 +380,9 @@ struct radeon_info {
    bool has_sparse_image_standard_3d;
    /* Mip levels do not need to be aligned to the sparse block size */
    bool has_sparse_unaligned_mip_size;
+   bool has_gang_submit;
    bool has_gpuvm_fault_query;
+   bool has_pcie_bandwidth_info;
    /* Whether SR-IOV is enabled or amdgpu.mcbp=1 was set on the kernel command line. */
    bool has_kernelq_reg_shadowing;
    bool has_default_zerovram_support;
diff --git a/src/amd/common/ac_surface.c b/src/amd/common/ac_surface.c
index 16a97a3c2d5..36436f83100 100644
--- a/src/amd/common/ac_surface.c
+++ b/src/amd/common/ac_surface.c
@@ -2100,6 +2100,10 @@ static bool gfx10_DCN_requires_independent_64B_blocks(const struct radeon_info *
 {
    assert(info->gfx_level >= GFX10);
 
+   /* Older kernels have buggy DAL. */
+   if (info->drm_minor <= 43)
+      return true;
+
    /* For 4K, DCN requires INDEPENDENT_64B_BLOCKS = 1 and MAX_COMPRESSED_BLOCK_SIZE = 64B. */
    return config->info.width > 2560 || config->info.height > 2560;
 }
diff --git a/src/amd/vulkan/radv_physical_device.c b/src/amd/vulkan/radv_physical_device.c
index 44d0703d79a..700e3784ae3 100644
--- a/src/amd/vulkan/radv_physical_device.c
+++ b/src/amd/vulkan/radv_physical_device.c
@@ -71,7 +71,8 @@ radv_taskmesh_enabled(const struct radv_physical_device *pdev)
    if (instance->debug_flags & RADV_DEBUG_NO_MESH_SHADER)
       return false;
 
-   return pdev->use_ngg && !pdev->use_llvm && pdev->info.gfx_level >= GFX10_3 && radv_compute_queue_enabled(pdev);
+   return pdev->use_ngg && !pdev->use_llvm && pdev->info.gfx_level >= GFX10_3 && radv_compute_queue_enabled(pdev) &&
+          pdev->info.has_gang_submit;
 }
 
 bool
@@ -115,7 +116,7 @@ radv_transfer_queue_enabled(const struct radv_physical_device *pdev)
        !(instance->experimental_flags & RADV_EXPERIMENTAL_TRANSFER_QUEUE))
       return false;
 
-   if (!radv_compute_queue_enabled(pdev))
+   if (!pdev->info.has_gang_submit || !radv_compute_queue_enabled(pdev))
       return false;
 
    return pdev->info.gfx_level >= GFX9 && !(instance->queue_disable_flags & RADV_QUEUE_DISABLE_TRANSFER);
diff --git a/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_bo.c b/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_bo.c
index 754d7889827..e83b57afde4 100644
--- a/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_bo.c
+++ b/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_bo.c
@@ -578,7 +578,7 @@ radv_amdgpu_winsys_bo_create(struct radeon_winsys *_ws, uint64_t size, unsigned
          request.flags |= AMDGPU_GEM_CREATE_VRAM_CLEARED;
    }
 
-   if (flags & RADEON_FLAG_DISCARDABLE)
+   if (flags & RADEON_FLAG_DISCARDABLE && ws->info.drm_minor >= 47)
       request.flags |= AMDGPU_GEM_CREATE_DISCARDABLE;
 
    if (flags & RADEON_FLAG_GFX12_ALLOW_DCC && ws->info.drm_minor >= 58) {
diff --git a/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_cs.c b/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_cs.c
index 3cd93257cbb..a1d59ab2426 100644
--- a/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_cs.c
+++ b/src/amd/vulkan/winsys/amdgpu/radv_amdgpu_cs.c
@@ -210,7 +210,8 @@ radv_amdgpu_cs_domain(const struct radeon_winsys *_ws)
     * If there is no PCIe info, assume there is enough bandwidth.
     */
    const uint32_t bandwidth_mbps_threshold = 8 * 0.985 * 1024;
-   bool enough_bandwidth = ws->info.pcie_bandwidth_mbps >= bandwidth_mbps_threshold;
+   bool enough_bandwidth =
+      !ws->info.has_pcie_bandwidth_info || ws->info.pcie_bandwidth_mbps >= bandwidth_mbps_threshold;
 
    bool use_sam =
       (enough_vram && enough_bandwidth && ws->info.has_dedicated_vram && !(ws->perftest & RADV_PERFTEST_NO_SAM)) ||
diff --git a/src/gallium/drivers/radeonsi/gfx/si_gfx_screen.c b/src/gallium/drivers/radeonsi/gfx/si_gfx_screen.c
index 4c002589fc5..b9a2b9115c1 100644
--- a/src/gallium/drivers/radeonsi/gfx/si_gfx_screen.c
+++ b/src/gallium/drivers/radeonsi/gfx/si_gfx_screen.c
@@ -269,6 +269,7 @@ static bool enable_mesh_shader(struct si_screen *sscreen)
 {
    return sscreen->use_ngg &&
       sscreen->info.gfx_level >= GFX10_3 &&
+      sscreen->info.has_gang_submit &&
       /* TODO: not support user queue for now */
       !(sscreen->info.userq_ip_mask & BITFIELD_BIT(AMD_IP_GFX)) &&
       /* don't support LLVM */
diff --git a/src/gallium/winsys/amdgpu/drm/amdgpu_cs.cpp b/src/gallium/winsys/amdgpu/drm/amdgpu_cs.cpp
index d015dba78e0..ff44030e06e 100644
--- a/src/gallium/winsys/amdgpu/drm/amdgpu_cs.cpp
+++ b/src/gallium/winsys/amdgpu/drm/amdgpu_cs.cpp
@@ -2415,6 +2415,7 @@ static void amdgpu_winsys_fence_reference(struct radeon_winsys *rws,
 static bool amdgpu_cs_create_compute_gang(struct radeon_cmdbuf *rcs)
 {
    struct amdgpu_cs *acs = amdgpu_cs(rcs);
+   assert(acs->aws->info.has_gang_submit);
    struct radeon_cmdbuf *gang;
    assert(acs);
 
